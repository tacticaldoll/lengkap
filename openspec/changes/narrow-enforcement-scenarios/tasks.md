# Tasks

## 1. Prose outside the specs

- [x] 1.1 In `AGENTS.md` Definition Of Done prose, say `lengkap-governance` observes normal dependencies, inline calls into selected standard-library paths, public async functions, inline `std::time` `now` calls, and serde marker acquisition
- [x] 1.2 In `BACKLOG.md` "Governance states only observable facts", use the same observed shapes
- [x] 1.3 Run the Definition Of Done and family-check
